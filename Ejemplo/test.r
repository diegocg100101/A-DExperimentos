# Asinación a la variable "x"
c(10.4, 5.6, 3.1, 6.4, 21.7) -> x

# Función para promedio
promedio = mean(x)

# Función para la varianza
varianza = var(x)

# La raíz cuadrada de la varianza es la desviación estándar
desviacion_estandar = sqrt(var(x))

# Tamaño del vector "x"
n = length(x)

y = x

v <- 2 * x + y + 1

c(10.4, 5.3, 5.7, 21.7) -> x

seq(-5, 5, by = .1) -> s3

hist(s3)