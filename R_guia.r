setwd("ruta/directorio")  # Cambia de directorio
getwd() # Obtiene la ruta actual
dir() # Obtiene todos los documentos que están dentro
mean(x) # promedio de un vector
x <- c(10.4, 5.3, 5.7) # Asignar un vector a "x"
assign("x", c(10.4, 5.3, 5.7)) # Asignar de otra manera
c(10.4, 5.3, 5.7)  -> d # Asignación hacia la derecha
y <- c(x, 0, x)

min(x)
max(x)
sort(x)
var(x) # varianza
sqrt(var(x)) # desviación estándar
